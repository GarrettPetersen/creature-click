{
  "name": "creature-click",
  "private": true,
  "scripts": {
    "dev": "npm run build:public && npx --yes serve public -l 3333",
    "build": "node scripts/generate-og-image.mjs && node scripts/build-public.mjs",
    "build:public": "node scripts/build-public.mjs",
    "deploy": "npm run build && wrangler versions upload",
    "generate:og": "node scripts/generate-og-image.mjs",
    "build:animals": "node scripts/build-animals-json.mjs",
    "verify:animals": "node scripts/verify-animal-sources.mjs",
    "harvest:animals": "node scripts/harvest-animals-wikidata.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "wrangler": "^4.22.0"
  }
}
